<%= form_for(@provider) do |f| %>
  <% if @provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@provider.errors.count, "error") %> prohibited this provider from being saved:</h2>

      <ul>
      <% @provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Nombre" %><br>
    <%= f.text_field :NombreProvider, :placeholder => "Nombre del Proveedor"  %>
  </div>
  <div class="field">
    <%= f.label :"Dirección" %><br>
    <%= f.text_field :direccionProvider, :placeholder => "Dirección"  %>
  </div>
  <div class="field">
    <%= f.label :"Fecha de Asociación " %><br>
    <%= f.date_select :fechadasoProvider %>
  </div>
  <div class="actions">
    <%= f.submit :"Aceptar" %>
  </div>
<% end %>
