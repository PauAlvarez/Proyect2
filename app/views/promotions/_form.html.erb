<%= form_for @promotion, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :fechapromo, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :fechapromo, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :mediodecomunica, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :mediodecomunica, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :descrippromo, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :descrippromo, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :acuerdo, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :acuerdo, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :user_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :user_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :client_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :client_id, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                promotions_path, :class => 'btn' %>
  </div>
<% end %>
