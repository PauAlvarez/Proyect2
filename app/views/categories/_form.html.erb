<%= form_for @category, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :NombreCategoria, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :NombreCategoria, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :descripcionCategoria, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :descripcionCategoria, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                categories_path, :class => 'btn' %>
  </div>
<% end %>
