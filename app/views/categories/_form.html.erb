<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :"Nombre" %><br>
    <%= f.text_field :nombreCategoria, :placeholder => "Nombre de la categoria" %>
  </div>
  <div class="field">
    <%= f.label :"Descripción" %><br>
    <%= f.text_area :descripcionCategoria, :placeholder => "Descripción" %>
  </div>
  <div class="actions">
    <%= f.submit :"Aceptar" %>
  </div>
<% end %>
