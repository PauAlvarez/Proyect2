<%= form_for @fee, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :cantidaddinero, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :cantidaddinero, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :fechaestablecida, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :fechaestablecida, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :user_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :user_id, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                fees_path, :class => 'btn' %>
  </div>
<% end %>
